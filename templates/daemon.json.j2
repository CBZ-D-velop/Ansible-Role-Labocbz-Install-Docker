{
{% if docker_insecure_registries | default(false) %}
"insecure-registries": [
{% for registry in docker_insecure_registries %}
{% if not loop.last %}
"{{ registry }}",
{% else %}
"{{ registry }}"
{% endif %}
{% endfor %}
]
{% endif %}
}